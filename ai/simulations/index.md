# Simulations

This is a registry, not a code folder. Numerical experiments' actual code
lives in `ai/code_dev/` (in-progress, `development` phase) or `code/`
(deployed, `deployment` phase) — this page just indexes them and links each
one to the investigation it supports.

| Date | Purpose | Linked investigation | Code path | Status |
|---|---|---|---|---|
