---
description: Start the live dashboard server
allowed-tools: Bash
---
Start the live dashboard in the background:
`python3 ai/scripts/serve.py` (default port 8000; pass `--port` if that port
is busy). Tell me the URL to open (http://127.0.0.1:8000/).
