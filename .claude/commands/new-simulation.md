---
description: Register a numerical experiment in the simulations index
allowed-tools: Read, Edit
---
Add a row to `ai/simulations/index.md` for a numerical experiment: $ARGUMENTS.

Record its purpose, linked investigation (if any), the path in
`ai/code_dev/` or `code/` where the actual code lives, and its status.

This is a registry entry only — there is no scratch-folder template to
scaffold. The code itself belongs in `ai/code_dev/` (development phase) or
`code/` (deployment phase), wherever the project's code already lives.
